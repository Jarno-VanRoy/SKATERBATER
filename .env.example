# ┌───────────────────────────────┐
# │   Flask Core Configuration   │
# └───────────────────────────────┘

# Secret key for securely signing session cookies
FLASK_SECRET_KEY=your-super-secret-key

# Environment mode: development | production
FLASK_ENV=development

# ┌───────────────────────────────┐
# │     Database Configuration    │
# └───────────────────────────────┘

# SQLAlchemy database URI
# Example: use SQLite for local development
DATABASE_URL=sqlite:///instance/skaterbater.db

# ┌───────────────────────────────┐
# │      Auth0 Configuration      │
# └───────────────────────────────┘

# Auth0 application credentials
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Your Auth0 tenant domain (e.g. dev-abc123.us.auth0.com)
AUTH0_DOMAIN=your-auth0-domain

# Callback URL after login — must match Auth0 app config
AUTH0_CALLBACK_URL=https://localhost:5000/callback
